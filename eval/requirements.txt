editdistance>=0.6.0
pandas>=2.0.0
openpyxl>=3.1.0
tqdm>=4.65.0

# OCR 推理后端（按需安装其中之一即可）
# 方式一：调用兼容 OpenAI 协议的 API（阿里云 DashScope / OpenRouter / 本地 vLLM）
openai>=1.30.0

# 方式二：本地 HuggingFace Transformers 推理
# torch>=2.1.0
# transformers>=4.45.0
# accelerate>=0.33.0
# Pillow>=10.0.0
